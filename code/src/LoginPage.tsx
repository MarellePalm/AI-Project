function LoginPage() {
    return (
        <>
            {Array(20).fill(1).map((x) => 
                <p className="text-8xl">bruh</p>
            )}
        </>
    )
}

export default LoginPage
